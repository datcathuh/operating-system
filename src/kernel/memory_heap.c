#include "memory.h"

void *mem_alloc(size_t size) { return NULL; }
void *mem_alloc_zero(size_t size) { return NULL; }
void mem_free(void *ptr) {}
size_t mem_alloc_usable_size(void *ptr) { return 0; }
